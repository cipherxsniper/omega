def apply_learning(memory, goal, score):

    init_goals(memory)

    g = memory["goals"][goal]

    if score > 0.7:
        g["priority"] = min(1.0, g["priority"] + 0.05)
    else:
        g["priority"] = max(0.1, g["priority"] - 0.03)

    g["progress"] = min(1.0, g["progress"] + score * 0.1)
